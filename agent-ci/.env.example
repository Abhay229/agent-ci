# Set to 1 to use real LLM calls via OpenRouter (requires API key below).
# Leave unset or 0 for mock mode (default, no API key needed).
AGENT_CI_LIVE=0

# Required only when AGENT_CI_LIVE=1
OPENROUTER_API_KEY=sk-or-your-key-here
